export const metadata = {
  title: "Team — Vexora",
  description: "Team page.",
};

export default function Page() {
  // Intentionally empty page body per request — navbar/title bar will still render via the global layout
  return <main />;
}
